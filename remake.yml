## All R files that are used must be listed here:
sources:
  - R.scripts/data_cleaning.R
  - R.scripts/data_organizing.R
  - R.scripts/run_analyses.R
  - R.scripts/results.R
  
packages:
  - dplyr
  - magrittr
  - lubridate
  - tidyr
  - mvabund
  - ggplot2
  - lubridate
  - vegan
  - pryr

targets:
  all:
    depends: 
      - adonis_list

  insects:
    command: read_data("raw-data/insect.communities.table.txt")
  
  insectnames:
    command: read.csv("raw-data/insectnames.csv", stringsAsFactors=FALSE)
    
  blocks:
    command: read_data("raw-data/blocks.txt")
    
  bromeliad:
    command: read_data("raw-data/bromeliad.volumes.txt")

  inverts:
    command: clean_insect_names(insects, insectnames)
    quiet: true

  zoops:
    command: clean_zooplankton("raw-data/zoop.txt")
    
  bact_1:
    command: read_clean_bact("raw-data/bacteria/block1.csv")
        
  bact_2:
    command: read_clean_bact("raw-data/bacteria/block2.csv")
        
  bact_3:
    command: read_clean_bact("raw-data/bacteria/block3.csv")
        
  bact_4:
    command: read_clean_bact("raw-data/bacteria/block4.csv")
        
  bact_5:
    command: read_clean_bact("raw-data/bacteria/block5.csv")
    
  meta_list:
    command: list(zoops = zoops, inverts = inverts)

  bact_list:
    command: >
      list(bact_1 = bact_1, bact_2 = bact_2, bact_3 = bact_3,
           bact_4 = bact_4, bact_5 = bact_5)
    
  meta_list_ini:
    command: lapply_meta(meta_list, .blocks = blocks, .bromeliad = bromeliad, sampletime = I("initial"))
    
  meta_list_fin:
    command: lapply_meta(meta_list, .blocks = blocks, .bromeliad = bromeliad, sampletime = I("final"))

  bact_list_ini:
    command: lapply_bact(bact_list, .blocks = blocks, .bromeliad = bromeliad, sampletime = I("initial"))   

  bact_list_fin:
    command: lapply_bact(bact_list, .blocks = blocks, .bromeliad = bromeliad, sampletime = I("final"))   

  meta_ini_adonis:
    command: lapply_adonis(meta_list_ini)
    
  meta_fin_adonis:
    command: lapply_adonis(meta_list_fin)
    
  bact_ini_adonis:
    command: lapply_adonis(bact_list_ini)
    
  bact_fin_adonis:
    command: lapply_adonis(bact_list_fin)
    
  adonis_list:
    command: list(meta_ini_adonis, meta_fin_adonis, bact_ini_adonis, bact_fin_adonis)

#  bactlist:
#    command: Select_bact_list_initial(.blocks = blocks, .bromeliad = bromeliad, .bact_list = bact_list_clean2)

#  list_initial:
#    command: InsectZooBactAbds(.insectlist = insectlist, .zooplist = zooplist, .bactlist = bactlist)
  
#  adonis_initial:
#    command: runadonis(list_initial)
